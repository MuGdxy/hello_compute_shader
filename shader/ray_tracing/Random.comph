#ifndef MU_RANDOM
#define MU_RANDOM
uint state;
uint xor_shift_rand();
void Seed(uvec2 screenCoord, uvec2 screenSize);
float Rand();
float Rand(float min_, float max_);
vec2 RandVec2(float min_, float max_);
vec3 RandVec3(float min_, float max_);
vec3 RandUnitBall();
vec2 RandDisk();
vec3 RandSemiUnitBall(vec3 normal);
vec3 RandUnitVector();
#endif